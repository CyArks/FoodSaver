import React from 'react';

const GroceryListComponent = ({ items }) => {
  return (
    <div>
      {/* Render your grocery list */}
      {/* Use the 'items' prop to display the list of grocery items */}
    </div>
  );
};

export default GroceryListComponent;
